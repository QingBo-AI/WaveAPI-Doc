---
title: "图像生成概述"
description: "通过 /v1/tasks 统一端点调用所有图像生成模型,异步任务异步取结果"
---

清波 API 支持多家图像生成模型,**统一通过异步任务接口** `/v1/tasks` 调用。图像生成通常 1-30 秒完成,提交后返回 `task_id`,通过轮询或 Webhook 回调获取结果。

## 请求端点

```
POST https://api.qingbo.ai/v1/tasks
```

异步流程详见 [任务系统](/cn/docs/task-system) — Submit → Status → Result + 可选 webhook。

## 支持的模型

<CardGroup cols={2}>
  <Card title="Gemini 图像系列" href="/cn/api-reference/image/gemini/overview">
    Nano Banana 官方版三档按实际 token 用量结算，经济版四档按张固定价，最多 14 张参考图
  </Card>
  <Card title="GPT Image 系列" href="/cn/api-reference/image/gpt-image/overview">
    GPT Image 1 / 1.5 / 2 / 2.5 官方版按实际 token 用量结算，GPT Image 2 / 2.5 逆向版按张固定价
  </Card>
  <Card title="Seedream 系列" href="/cn/api-reference/image/seedream">
    字节跳动 Seedream 4.0 / 4.5 / 5.0 Lite / 5.0 Pro / 5.0 Flash，文生图与参考图编辑
  </Card>
  <Card title="Qwen Image 系列" href="/cn/api-reference/image/qwen-image">
    阿里通义千问 Qwen Image 2.0 / 2.0 Pro / 3.0 / 3.0 Pro，文字渲染强
  </Card>
  <Card title="Wan Image 系列" href="/cn/api-reference/image/wan-image">
    阿里通义万相 Wan 2.7 Image / Image Pro，文生图、参考图编辑、框选编辑与配色主题
  </Card>
  <Card title="Z-Image" href="/cn/api-reference/image/z-image">
    Z-Image-Turbo，轻量快速，中英双语
  </Card>
  <Card title="FLUX 系列" href="/cn/api-reference/image/flux">
    Black Forest Labs FLUX.2 Pro / Max / Flex 与 Kontext Pro / Max，文生图与参考图编辑
  </Card>
  <Card title="Grok Imagine 系列" href="/cn/api-reference/image/grok-imagine">
    xAI Grok Imagine 1.5 逆向档、2.0 官方与逆向档、高质量档，文生图与参考图编辑
  </Card>
  <Card title="Midjourney" href="/cn/api-reference/image/midjourney">
    imagine 一次出四张，放大、变体、缩放、平移、重绘等 12 种链式动作
  </Card>
</CardGroup>

## 两条调用路径

图像有两个入口，计费完全相同，差别在参数校验和返回方式：

| | `POST /v1/tasks` | `POST /v1/images/generations` · `/v1/images/edits` |
|---|---|---|
| 返回 | 异步，先拿 `task_id` 再查结果（可用 `Prefer: wait` 在一次调用里等） | 同步，直接返回图片 |
| 请求形状 | 任务接口的统一字段：`action` / `aspect_ratio` / `resolution` / `image_urls` | OpenAI 原生形状：`size` / `quality` / `n` / `response_format` |
| 参数校验 | **完整**——动作、画幅、分辨率、画质、参考图张数都会校验，不合规直接返回 `400`，不计费 | 兼容层不做这层校验，参数不合规不会被提前拦下 |
| 适合 | 新接入、需要掩码重绘 / 画质档 / 分辨率档 / 多图参考的场景 | 已有 OpenAI SDK 的项目，改 `base_url` 就能跑 |

<Note>
用 OpenAI SDK 迁移时，把 `base_url` 指向 `https://api.qingbo.ai/v1`，`model` 换成对应的模型 ID 即可。
各模型独有的能力（掩码 `mask_url`、`1k` / `2k` / `4k` 分辨率档、参考图张数上限等）以任务接口的文档为准，
各模型页描述的都是任务接口的参数。
</Note>

## 通用参数(全系列共有)

> 各模型实际支持范围不同,**详见各 vendor 文档**。

<ParamField body="model" type="string" required>
  模型 ID(group_name),从上面的 vendor 文档选择
</ParamField>

<ParamField body="action" type="string" default="generate">
  - `generate` — 文生图(默认)
  - `edit` — 图像编辑,配合 `image_urls` 使用

  **模式不靠 action 区分,靠传什么:** 只给 `prompt` 是文生图;带一张 `image_urls` 是图生图;带多张是多图参考融合;再带 `mask_url` 就是局部重绘。
</ParamField>

<ParamField body="prompt" type="string" required>
  图像描述文本,支持中英文
</ParamField>

<ParamField body="n" type="integer" default="1">
  生成数量(部分 vendor 限制单次张数)
</ParamField>

<ParamField body="seed" type="integer" default="-1">
  随机种子,`-1` 为随机;固定值可复现相似结果
</ParamField>

<ParamField body="aspect_ratio" type="string">
  画面宽高比,如 `16:9` / `9:16` / `1:1`,部分 vendor 支持 `auto`(智能选择)
</ParamField>

<ParamField body="resolution" type="string">
  输出分辨率,如 `1K` / `2K` / `4K`,各 vendor 支持范围不同
</ParamField>

<ParamField body="image_urls" type="string[]">
  参考图片 URL 数组(图生图 / 多图参考用)
</ParamField>

<ParamField body="callback_url" type="string">
  Webhook 回调地址,任务终态时调用。详见 [任务系统](/cn/docs/task-system#webhook)
</ParamField>

## 提交响应示例

```json
{
  "task_id": "task-wave1775285160b950328499",
  "model": "seedream-5.0-lite",
  "action": "generate",
  "status": "queued",
  "created_at": 1775285160040,
  "progress": 0
}
```

拿到 `task_id` 后调 [`GET /v1/tasks/{task_id}`](/cn/api-reference/task/status) 轮询,直到 `status = completed` 取图片 URL。

## 模式速查

| 模式 | 触发字段 | 典型 vendor |
|---|---|---|
| **文生图(T2I)** | 仅 `prompt` | 全部 |
| **图生图 / 多图参考** | `+ image_urls`(单图或多图) | Seedream / Gemini / GPT Image / Wan / Qwen / FLUX / Grok |
| **图像编辑(局部重绘 / 光影修改)** | `+ image_urls + action: edit` | Seedream / Gemini / GPT Image / Wan / Qwen / FLUX / Grok |
| **局部重绘(带 mask)** | `+ image_urls + mask_url + action: edit` | [GPT Image 官方版](/cn/api-reference/image/gpt-image/overview)各模型;GPT Image 逆向版与 Nano Banana 经济版**不支持** |
| **组图生成(主题相关一组)** | `enable_sequential: true` | Wan Image |
| **交互式编辑(bbox 框选)** | `+ image_urls + bbox_list` | Wan Image |

<Note>
并非所有模型都支持全部模式 — 各 vendor 实际支持的 `action` 列表 + 字段范围请查看对应文档。后端会校验请求字段是否在 vendor 声明的能力范围内,不在范围会返回错误。
</Note>

## 字段命名规范

- **媒体引用统一复数** — 一律 `image_urls`,即使单张也用单元素数组 `["one.jpg"]`
- **画面比例统一** `aspect_ratio` — vendor 内部用 `size` / `ratio` 等是实现细节,无需感知
- **分辨率统一** `resolution` — vendor 内部用 `quality` 等是实现细节
- **遮罩字段** `mask_url`(GPT Image 官方版局部重绘用)

## 相关文档

- [任务系统](/cn/docs/task-system) — 任务状态机 / 轮询节奏 / Webhook
- [请求响应格式](/cn/docs/request-response) — 通用错误码 / Headers / 限流
- [认证](/cn/docs/authentication) — API Key 申请与使用
- [模型列表](/cn/docs/models) — 全模型查询接口
